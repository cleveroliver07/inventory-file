on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:

    

      - name: Generate ansible.cfg
        run: |
          sudo -u oliver bash -c 'cat > /home/oliver/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/oliver/ansible/inventory.yaml
          roles_path    = /home/oliver/ansible/roles
          host_key_checking = False
          remote_user = oliver
          private_key_file = /home/oliver/.ssh/id_rsa
          log_path = /home/oliver/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/oliver/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/oliver/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown oliver:oliver /home/oliver/ansible/ansible.cfg
          sudo chmod 644 /home/oliver/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u oliver ls -la /home/oliver/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u oliver cat /home/oliver/ansible/ansible.cfg
